# STM32 两轮自平衡小车 — 规划方案

---

## 一、项目概述

### 1.1 项目目标

针对两轮自平衡小车稳定性差与调参复杂的问题，设计并实现基于 STM32 的双闭环控制系统。基于姿态传感器实时获取倾角数据，构建角度环 + 速度环级联控制模型，实现小车动态平衡与稳定运动。支持串口在线参数调整与状态回传，提升系统调试效率与可控性。

### 1.2 技术栈

- C语言、STM32、PID控制算法、PWM驱动、I2C通信、串口通信、定时器中断、互补滤波

### 1.3 核心指标

| 指标 | 目标值 |
|------|--------|
| 平衡角度范围 | ±30° |
| 角度解算频率 | 200Hz（5ms） |
| 速度环控制周期 | 50Hz（20ms） |
| PWM 输出频率 | 10kHz |
| 串口波特率 | 115200 |
| 姿态融合互补滤波系数 α | 0.98 |

---

## 二、系统架构

### 2.1 整体框图

```
┌─────────────────────────────────────────────────────────────┐
│                        系统架构                              │
│                                                             │
│  ┌──────────┐    ┌──────────┐    ┌──────────┐               │
│  │ MPU6050  │───>│ 姿态解算  │───>│ 角度环   │               │
│  │ (I2C)    │    │ 互补滤波  │    │ (PID)    │               │
│  └──────────┘    └──────────┘    └────┬─────┘               │
│                                       │                     │
│  ┌──────────┐    ┌──────────┐    ┌────▼─────┐               │
│  │ 编码器   │───>│ 速度计算  │───>│ 速度环   │               │
│  │ (定时器) │    │          │    │ (PID)    │               │
│  └──────────┘    └──────────┘    └────┬─────┘               │
│                                       │                     │
│                                  ┌────▼─────┐               │
│                                  │ PWM 输出  │               │
│                                  │ 电机驱动  │               │
│                                  └──────────┘               │
│                                                             │
│  ┌──────────┐    ┌──────────────────────────┐               │
│  │ 上位机   │<──>│ 串口通信 (参数调节/状态回传)│              │
│  │ (PC)     │    │                          │               │
│  └──────────┘    └──────────────────────────┘               │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 双闭环控制模型

```
速度设定值(0) ──> [速度环PID] ──> 角度设定值 ──> [角度环PID] ──> PWM输出 ──> 电机
      ^                                      |
      |                                      |
  编码器反馈 <── 速度计算                    MPU6050 <── 互补滤波 <── 姿态采集
```

**控制逻辑：**
- 外环（速度环）：以目标速度与实际速度的偏差作为输入，输出角度补偿值
- 内环（角度环）：以（目标角度 + 速度环输出）与实际倾角的偏差作为输入，输出 PWM 占空比
- 角度环响应快（5ms），速度环响应慢（20ms），形成级联控制

---

## 三、硬件资源分配

### 3.1 STM32 引脚分配

| 功能 | 外设/引脚 | 说明 |
|------|-----------|------|
| MPU6050 SCL | PB6 (I2C1_SCL) | I2C 时钟线 |
| MPU6050 SDA | PB7 (I2C1_SDA) | I2C 数据线 |
| 左电机 PWM | PA0 (TIM2_CH1) | PWM 输出 |
| 右电机 PWM | PA1 (TIM2_CH2) | PWM 输出 |
| 左电机方向 | PB0, PB1 | GPIO 控制 H 桥 |
| 右电机方向 | PB10, PB11 | GPIO 控制 H 桥 |
| 左编码器 A/B | PA6/PA7 (TIM3) | 正交编码器 |
| 右编码器 A/B | PA8/PA9 (TIM1) | 正交编码器 |
| USART TX | PA2 (USART2_TX) | 串口发送 |
| USART RX | PA3 (USART2_RX) | 串口接收 |

### 3.2 定时器资源分配

| 定时器 | 用途 | 频率/周期 |
|--------|------|-----------|
| TIM2 | PWM 输出（电机） | 10kHz |
| TIM3 | 左编码器接口 | 硬件编码器模式 |
| TIM1 | 右编码器接口 | 硬件编码器模式 |
| TIM4 | 系统调度（定时中断） | 200Hz (5ms) |
| SysTick | 系统时基 / 延时 | 1ms |

---

## 四、软件模块设计

### 4.1 目录结构

```
new_project/
├── Core/
│   ├── Inc/                    # 头文件
│   │   ├── main.h
│   │   ├── stm32f1xx_hal_conf.h
│   │   └── stm32f1xx_it.h
│   ├── Src/                    # 源文件
│   │   ├── main.c
│   │   ├── stm32f1xx_it.c     # 中断服务函数
│   │   └── system_stm32f1xx.c
│   └── Startup/
│       └── startup_stm32f103xb.s
├── Drivers/                    # HAL 库驱动
│   ├── CMSIS/
│   └── STM32F1xx_HAL_Driver/
├── App/                        # 应用层代码
│   ├── Inc/
│   │   ├── mpu6050.h           # MPU6050 驱动
│   │   ├── imu.h               # 姿态解算（互补滤波）
│   │   ├── pid.h               # PID 控制器
│   │   ├── motor.h             # 电机驱动
│   │   ├── encoder.h           # 编码器
│   │   ├── uart_protocol.h     # 串口协议
│   │   └── scheduler.h         # 任务调度
│   └── Src/
│       ├── mpu6050.c
│       ├── imu.c
│       ├── pid.c
│       ├── motor.c
│       ├── encoder.c
│       ├── uart_protocol.c
│       └── scheduler.c
└── MDK-ARM/                    # Keil 工程文件
```

### 4.2 各模块详细设计

---

#### 模块 1：MPU6050 驱动 (`mpu6050.c / .h`)

**功能：** 通过 I2C 读取加速度计和陀螺仪原始数据。

**关键接口：**
```c
// 初始化 MPU6050（采样率、量程、低通滤波）
void MPU6050_Init(I2C_HandleTypeDef *hi2c);

// 测试连接（读取 WHO_AM_I）
uint8_t MPU6050_TestConnection(void);

// 读取原始数据
void MPU6050_ReadRaw(MPU6050_Data_t *data);

// 读取并校准（减去零偏）
void MPU6050_ReadCalibrated(MPU6050_Data_t *data);

// 上电校准（静止状态下采集零偏）
void MPU6050_Calibrate(MPU6050_Data_t *data, uint16_t samples);
```

**数据结构：**
```c
typedef struct {
    int16_t accel_x, accel_y, accel_z;    // 原始加速度
    int16_t gyro_x, gyro_y, gyro_z;       // 原始角速度
    float accel_x_offset, accel_y_offset, accel_z_offset;  // 零偏
    float gyro_x_offset, gyro_y_offset, gyro_z_offset;
} MPU6050_Data_t;
```

**关键寄存器配置：**
| 寄存器 | 地址 | 配置值 | 说明 |
|--------|------|--------|------|
| SMPLRT_DIV | 0x19 | 0x07 | 采样率 = 1kHz/(7+1) = 125Hz |
| CONFIG | 0x1A | 0x06 | 低通滤波 5Hz |
| GYRO_CONFIG | 0x1B | 0x18 | ±2000°/s |
| ACCEL_CONFIG | 0x1C | 0x01 | ±4g |
| PWR_MGMT_1 | 0x6B | 0x01 | PLL with X-axis gyro |

---

#### 模块 2：姿态解算 (`imu.c / .h`)

**功能：** 互补滤波融合加速度计与陀螺仪数据，计算实时倾角。

**算法公式：**
```
角度_加速度 = atan2(ay, az) * RAD_TO_DEG
角度_陀螺仪 = 上次角度 + gyro_x * dt
互补滤波角度 = α × 角度_陀螺仪 + (1-α) × 角度_加速度
```
其中 α = 0.98，dt = 0.005s（200Hz）

**关键接口：**
```c
typedef struct {
    float angle;        // 融合后的倾角（度）
    float gyro;         // 角速度（°/s）
    float accel_angle;  // 加速度计计算的角度
    float dt;           // 采样周期
} IMU_Data_t;

void IMU_Init(IMU_Data_t *imu, float dt);
void IMU_Update(IMU_Data_t *imu, float accel_angle, float gyro_rate);
```

---

#### 模块 3：PID 控制器 (`pid.c / .h`)

**功能：** 通用位置式 PID 控制器，支持双环控制。

**位置式 PID 公式：**
```
output = Kp * error + Ki * integral + Kd * derivative
```

**关键接口：**
```c
typedef struct {
    float Kp, Ki, Kd;      // PID 参数
    float target;           // 目标值
    float error;            // 当前误差
    float last_error;       // 上次误差
    float integral;         // 积分累积
    float integral_limit;   // 积分限幅
    float output_limit;     // 输出限幅
    float output;           // 控制输出
} PID_Controller_t;

void PID_Init(PID_Controller_t *pid, float kp, float ki, float kd);
void PID_SetTarget(PID_Controller_t *pid, float target);
float PID_Calculate(PID_Controller_t *pid, float measured);
void PID_Reset(PID_Controller_t *pid);
void PID_SetParams(PID_Controller_t *pid, float kp, float ki, float kd);
void PID_SetLimits(PID_Controller_t *pid, float integral_limit, float output_limit);
```

**积分抗饱和：** 积分项限幅为 ±integral_limit，输出限幅为 ±output_limit。

**双环 PID 实例：**
```c
PID_Controller_t pid_angle;   // 角度环：Kp=30, Ki=0, Kd=5 (初始值，需调试)
PID_Controller_t pid_speed;   // 速度环：Kp=0.5, Ki=0.01, Kd=0 (初始值，需调试)
```

---

#### 模块 4：电机驱动 (`motor.c / .h`)

**功能：** 通过 PWM + GPIO 控制电机转速和方向。

**关键接口：**
```c
void Motor_Init(TIM_HandleTypeDef *htim);
void Motor_SetPWM(int16_t left_pwm, int16_t right_pwm);  // 范围 -999 ~ +999
void Motor_Stop(void);
void Motor_SetDeadzone(int16_t deadzone);
```

**PWM 配置：**
- TIM2, 通道 1/2
- 预分频：72-1 → 1MHz 计数频率
- 自动重载：100-1 → 10kHz PWM 频率
- 占空比范围：0 ~ 999

**方向控制真值表：**
| 左电机 | PB0 | PB1 | 动作 |
|--------|-----|-----|------|
| 正转 | 1 | 0 | 前进 |
| 反转 | 0 | 1 | 后退 |
| 刹车 | 1 | 1 | 制动 |
| 滑行 | 0 | 0 | 停止 |

---

#### 模块 5：编码器 (`encoder.c / .h`)

**功能：** 通过定时器硬件编码器模式读取电机转速。

**关键接口：**
```c
void Encoder_Init(TIM_HandleTypeDef *htim_left, TIM_HandleTypeDef *htim_right);
int16_t Encoder_ReadLeft(void);    // 读取并清零左编码器计数
int16_t Encoder_ReadRight(void);   // 读取并清零右编码器计数
float Encoder_GetSpeed(void);      // 返回归一化速度值
```

**速度计算：**
```
速度 = (左编码器 + 右编码器) × 0.5
```

---

#### 模块 6：串口协议 (`uart_protocol.c / .h`)

**功能：** 与上位机通信，实现参数在线调节和状态回传。

**协议格式：**

上位机下发（参数调整）：
```
帧头(0xAA) | 命令字 | 数据长度 | 数据 | 校验和 | 帧尾(0x55)
```

| 命令字 | 功能 | 数据格式 |
|--------|------|----------|
| 0x01 | 设置角度环 Kp | 4字节 float |
| 0x02 | 设置角度环 Ki | 4字节 float |
| 0x03 | 设置角度环 Kd | 4字节 float |
| 0x04 | 设置速度环 Kp | 4字节 float |
| 0x05 | 设置速度环 Ki | 4字节 float |
| 0x06 | 设置速度环 Kd | 4字节 float |
| 0x10 | 设置目标速度 | 4字节 float |
| 0x20 | 读取当前参数 | 无数据 |
| 0x30 | 读取实时状态 | 无数据 |

下位机回传（状态反馈）：
```
帧头(0xAA) | 命令字 | 数据长度 | 数据 | 校验和 | 帧尾(0x55)
```

| 命令字 | 功能 | 数据格式 |
|--------|------|----------|
| 0x81 | 当前参数 | 角度Kp/Ki/Kd + 速度Kp/Ki/Kd (24字节) |
| 0x82 | 实时状态 | 角度+速度+PWM+编码器 (16字节) |
| 0x83 | ACK 确认 | 1字节状态码 |

**关键接口：**
```c
void UART_Proc_Init(UART_HandleTypeDef *huart);
void UART_Proc_Process(void);           // 在主循环中调用，处理接收数据
void UART_Proc_SendStatus(float angle, float gyro, float speed, int16_t pwm_left, int16_t pwm_right);
void UART_Proc_SendParams(float angle_kp, float angle_ki, float angle_kd,
                          float speed_kp, float speed_ki, float speed_kd);
```

---

#### 模块 7：任务调度 (`scheduler.c / .h`)

**功能：** 基于 TIM4 定时中断的周期性任务调度。

**调度方案：**

| 任务 | 周期 | 优先级 | 说明 |
|------|------|--------|------|
| 姿态采集与解算 | 5ms (200Hz) | 最高 | MPU6050 读取 + 互补滤波 |
| 角度环 PID | 5ms (200Hz) | 高 | 与姿态同步 |
| 速度环 PID | 20ms (50Hz) | 中 | 编码器读取 + 速度计算 |
| 串口处理 | 50ms (20Hz) | 低 | 协议解析与数据回传 |

**实现方式：**
```c
typedef struct {
    void (*task_func)(void);   // 任务函数指针
    uint16_t period_ms;        // 执行周期（ms）
    uint16_t elapsed_ms;       // 已过时间（ms）
    uint8_t  enabled;          // 是否使能
} Scheduler_Task_t;

void Scheduler_Init(void);
void Scheduler_AddTask(void (*task_func)(void), uint16_t period_ms, uint8_t enabled);
void Scheduler_Run(void);       // 在 TIM4 中断中调用
```

**TIM4 中断服务流程（5ms 周期）：**
```
TIM4_IRQHandler()
  └── Scheduler_Run()
        ├── elapsed_ms += 5
        ├── if (elapsed_ms >= task.period_ms) → 执行 task.task_func()
        └── 重置 elapsed_ms
```

---

## 五、主程序流程

### 5.1 初始化流程 (`main.c`)

```
main()
  ├── HAL_Init()
  ├── SystemClock_Config()     // 72MHz
  ├── MX_GPIO_Init()          // 电机方向引脚
  ├── MX_I2C1_Init()          // MPU6050
  ├── MX_TIM2_Init()          // PWM
  ├── MX_TIM1_Init()          // 右编码器
  ├── MX_TIM3_Init()          // 左编码器
  ├── MX_TIM4_Init()          // 调度定时器
  ├── MX_USART2_Init()        // 串口
  ├── MPU6050_Init()
  ├── MPU6050_Calibrate()     // 静止校准（约2秒）
  ├── IMU_Init()
  ├── PID_Init(&pid_angle, 30, 0, 5)
  ├── PID_Init(&pid_speed, 0.5, 0.01, 0)
  ├── Motor_Init()
  ├── Encoder_Init()
  ├── UART_Proc_Init()
  ├── Scheduler_Init()
  ├── Scheduler_AddTask(Task_ImuUpdate,     5, 1)   // 5ms  - 姿态解算
  ├── Scheduler_AddTask(Task_AngleControl,  5, 1)   // 5ms  - 角度环
  ├── Scheduler_AddTask(Task_SpeedControl, 20, 1)   // 20ms - 速度环
  ├── Scheduler_AddTask(Task_UartReport,   50, 1)   // 50ms - 串口回传
  ├── HAL_TIM_Base_Start_IT()  // 启动 TIM4 中断
  └── while(1)
        ├── UART_Proc_Process()   // 串口处理
        └── __WFI()               // 等待中断，低功耗
```

### 5.2 中断驱动的控制循环

```
TIM4_IRQHandler (5ms)
  ├── 读取 MPU6050 原始数据
  ├── 互补滤波 → 更新倾角
  ├── 角度环 PID 计算 → angle_output
  │
  ├── [每4次 = 20ms] 速度环
  │     ├── 读取编码器 → 计算速度
  │     ├── 速度环 PID 计算 → speed_output
  │     └── 角度目标 = 0° + speed_output
  │
  ├── 最终 PWM = angle_output
  ├── Motor_SetPWM(PWM_left, PWM_right)
  │
  └── [每10次 = 50ms] 串口状态回传
```

---

## 六、核心代码框架

### 6.1 定时器中断中的控制主循环

```c
/* 任务函数 */
static void Task_ImuUpdate(void)
{
    MPU6050_ReadCalibrated(&mpu_data);
    
    /* 加速度计计算倾角（绕 X 轴旋转） */
    float accel_angle = atan2f(mpu_data.accel_y, mpu_data.accel_z) * RAD_TO_DEG;
    
    /* 互补滤波融合 */
    IMU_Update(&imu, accel_angle, mpu_data.gyro_x);
}

static void Task_AngleControl(void)
{
    /* 角度环：目标角度 → PWM */
    PID_SetTarget(&pid_angle, angle_target);
    pwm_output = (int16_t)PID_Calculate(&pid_angle, imu.angle);
    
    Motor_SetPWM(pwm_output, pwm_output);
}

static void Task_SpeedControl(void)
{
    /* 读取编码器速度 */
    float speed = Encoder_GetSpeed();
    
    /* 速度环：目标速度 → 角度补偿 */
    PID_SetTarget(&pid_speed, speed_target);
    float angle_comp = PID_Calculate(&pid_speed, speed);
    
    /* 修正角度目标 */
    angle_target = 0.0f + angle_comp;
}

static void Task_UartReport(void)
{
    float speed = Encoder_GetSpeed();
    UART_Proc_SendStatus(imu.angle, imu.gyro, speed, pwm_output, pwm_output);
}
```

---

## 七、PID 参数调试指南

### 7.1 调参步骤

**第一步：调试角度环（先让小车站住）**
1. 将速度环 Kp/Ki/Kd 全部设为 0
2. 角度环 Kp 从 0 开始逐步增大，直到小车能抵抗轻微倾斜
3. 加入 Kd 抑制振荡，从小值开始增大
4. Ki 一般设为 0 或极小值（角度环不需要消除稳态误差）

**参考初始值：**
```
角度环：Kp = 25~40, Ki = 0, Kd = 3~8
```

**第二步：调试速度环（让小车保持位置）**
1. 角度环参数固定
2. 速度环 Kp 从小值开始增大，直到小车能恢复位置
3. 加入 Ki 消除位置漂移
4. Kd 通常设为 0 或极小值

**参考初始值：**
```
速度环：Kp = 0.3~0.8, Ki = 0.005~0.02, Kd = 0
```

### 7.2 常见问题排查

| 现象 | 可能原因 | 调整方向 |
|------|----------|----------|
| 小车来回大幅摇摆 | 角度环 Kp 过大 | 减小角度环 Kp |
| 小车反应迟钝倒下 | 角度环 Kp 过小 | 增大角度环 Kp |
| 小车站住但持续抖动 | 角度环 Kd 过大 | 减小角度环 Kd |
| 小车缓慢漂移 | 速度环 Ki 过小或为 0 | 适当增大速度环 Ki |
| 小车原地转圈 | 左右电机参数不一致 | 校准电机死区补偿 |
| 加速度计数据噪声大 | 机械振动 | 增加减振措施，增大滤波系数 α |

---

## 八、测试计划

### 8.1 单元测试

| 测试项 | 方法 | 预期结果 |
|--------|------|----------|
| MPU6050 读取 | 读 WHO_AM_I | 返回 0x68 |
| 加速度计 | 水平放置读取 | az ≈ 1g, ax≈0, ay≈0 |
| 陀螺仪 | 静止读取 | gx/gy/gz ≈ 0（±几度/秒） |
| 互补滤波 | 倾斜 45° | 角度输出 ≈ 45° |
| PWM 输出 | 示波器测量 | 10kHz，占空比可调 |
| 编码器 | 手动转动车轮 | 脉冲计数随转速变化 |
| 串口通信 | 发送测试帧 | 正确解析并 ACK |

### 8.2 集成测试

| 测试项 | 方法 | 预期结果 |
|--------|------|----------|
| 静态平衡 | 手扶站立后松手 | 平衡 ≥ 30 秒 |
| 抗干扰 | 轻推小车 | 3 秒内恢复平衡 |
| 速度环 | 推动小车移动 | 自动回到原位 |
| 串口调参 | 运行中修改 Kp | 控制行为实时变化 |

---

## 九、开发计划

### 9.1 阶段划分

| 阶段 | 时间 | 内容 | 里程碑 |
|------|------|------|--------|
| **阶段 1** | 第 1~2 天 | 工程搭建与硬件验证 | I2C 读到 MPU6050 的 WHO_AM_I = 0x68 |
| **阶段 2** | 第 3~4 天 | MPU6050 驱动与姿态解算 | 串口打印出正确的倾角数据 |
| **阶段 3** | 第 5~6 天 | 电机驱动与编码器 | 串口控制电机正反转，读取编码器 |
| **阶段 4** | 第 7~10 天 | 角度环 PID 调试 | 小车能短暂保持平衡 |
| **阶段 5** | 第 11~13 天 | 速度环 PID 调试 | 小车能长时间稳定站立 |
| **阶段 6** | 第 14~15 天 | 串口调参与状态回传 | 上位机在线调参 |
| **阶段 7** | 第 16~17 天 | 系统集成与优化 | 完整功能演示 |

### 9.2 各阶段详细任务

**阶段 1：工程搭建**
- [ ] 使用 STM32CubeMX 创建工程，配置时钟 72MHz
- [ ] 配置 I2C1（PB6/PB7，400kHz Fast Mode）
- [ ] 配置 USART2（PA2/PA3，115200-8N1）
- [ ] 编写 I2C 扫描程序，验证 MPU6050 地址（0x68）
- [ ] 读取 WHO_AM_I 寄存器确认通信正常

**阶段 2：姿态解算**
- [ ] 实现 MPU6050 初始化与校准函数
- [ ] 实现原始数据读取（加速度 + 角速度）
- [ ] 实现互补滤波算法
- [ ] 串口以 50Hz 打印倾角数据，与实际倾斜对比验证

**阶段 3：电机与编码器**
- [ ] 配置 TIM2 为 PWM 模式（PA0/PA1，10kHz）
- [ ] 编写电机方向 GPIO 初始化
- [ ] 实现 Motor_SetPWM 函数
- [ ] 配置 TIM1/TIM3 为编码器接口模式
- [ ] 实现速度读取函数，验证脉冲计数方向正确

**阶段 4：角度环调试**
- [ ] 实现 PID 控制器（位置式）
- [ ] 将角度环 PID 输出映射到 PWM
- [ ] 手持小车测试 PID 响应
- [ ] 逐步调整 Kp → Kd，实现静态平衡

**阶段 5：速度环调试**
- [ ] 在角度环基础上叠加速度环
- [ ] 速度环输出作为角度环目标值的补偿
- [ ] 调整速度环 Kp → Ki
- [ ] 测试小车抗干扰能力（轻推）

**阶段 6：串口通信**
- [ ] 实现串口协议帧解析（帧头、命令、校验）
- [ ] 实现参数在线修改功能
- [ ] 实现状态数据周期回传
- [ ] 编写简单的 PC 端调试工具（Python/C#）

**阶段 7：集成优化**
- [ ] 整合所有模块，构建调度器
- [ ] 加入电机死区补偿
- [ ] 整体联调与性能测试
- [ ] 编写项目文档

---

## 十、风险与对策

| 风险 | 影响 | 对策 |
|------|------|------|
| MPU6050 数据漂移 | 姿态不准 | 上电校准 + 互补滤波抑制 |
| 电机死区过大 | 低速无法启动 | 加入死区补偿（PWM 最小值 ±50） |
| I2C 通信不稳定 | 数据读取失败 | 加入超时检测与重试机制 |
| 机械结构不稳 | 振动影响传感器 | 增加减振垫，优化重心位置 |

---

## 十一、工具与环境

| 工具 | 用途 |
|------|------|
| STM32CubeMX | 工程创建与外设配置 |
| Keil MDK / STM32CubeIDE | 代码编写与编译 |
| ST-Link | 程序下载与调试 |
| 串口调试助手 | 命令收发与数据查看 |
| 示波器 | PWM 波形与信号验证 |
| Python + pyserial | 上位机调试工具（可选） |

---

**文档版本**：v2.0
